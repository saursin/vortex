[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "vxdebug"
version = "0.1.0"
description = "Vortex Debugger"
readme = "README.md"
requires-python = ">=3.8"

[project.scripts]
vxdbg = "vxdebug.debugger:main"
